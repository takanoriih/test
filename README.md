# 半期粗利計算ツール (Next.js)

## セットアップ

Node.js をインストール後、以下を実行してください。

```bash
cd gross-profit-nextjs
npm install
npm run dev
```

ブラウザで http://localhost:3000 を開くと使えます。

## ビルド（本番）

```bash
npm run build
npm start
```
